# SERVICE · 주차장 담당자 Agent

## 1. 사용자·문제·가치
- 누구를 위한 서비스인가: 회사 사옥 주차장을 이용하는 임직원 (50~200명 규모) + 주차 관리 담당자
  - 일반 임직원: 본인/타 부서 차량의 입출입·잔여 대수·요금 조회
  - 담당자: 위 조회에 더해, 등록 차량 전체(차량번호·부서·차주 이름) 조회 및 예약 승인 처리
  - 주의: 현재 채팅창에는 로그인/권한 구분이 없어 "담당자 전용" 기능도 임직원이 접근 가능한 상태다. 이번 범위에서는 이 문제를 해결하지 않고 알려진 한계로 남긴다
- 어떤 문제를 푸는가: 지금 주차 가능한지, 특정 차량이 언제 들어오고 나갔는지 알기 어렵다
- 지금은 어떻게 해결하나: (가정) 차단기/번호인식 시스템은 있지만 상시 확인 체계는 없고, 필요할 때 담당자가 조회해서 알려준다
- 이걸로 뭐가 좋아지나: 물어보면 바로 잔여 주차 대수와 차량 입출입 이력이 나온다

## 2. 서비스 확장 관점
- 사내 기여: 담당자에게 매번 개별 문의하는 시간을 줄인다
- 규모: 사내 임직원 50~200명이 필요할 때만 가끔 사용
- 대체제와 차별점: 기존에는 담당자에게 직접 물어봐야 했지만, 이 에이전트는 대화형으로 즉시 답한다

## 3. 사용 예상 도구·데이터
- 구조: langgraph-supervisor로 감독자 에이전트가 조회/요금/관리자 하위 에이전트에게 위임하는 멀티 에이전트(agent.py)
- 도구(tools.py)
  - 조회(일반): 잔여 주차 대수 조회, 특정 차량 입출입 이력 조회, 부서명으로 조회, 최근 입차 차량 조회 — 차주 이름은 노출하지 않음
  - 조회(RAG): search_vehicles_semantic — 전기차/SUV처럼 정확한 필드값이 아닌 자연어 표현으로 차량을 찾음
  - 요금: 주차 요금 계산
  - 관리자 전용: 등록 차량 목록 조회(차량번호·부서·차주 이름 포함), 정기권/방문객/일반(임직원) 구분 필터 지원
  - 후순위(이번 범위 제외): 자동 알림 기능, 예약 승인 처리
- 데이터 적재(retriever.py): 고정된 더미 데이터 파일(data/carlist.json)을 매 호출마다 다시 읽어 그대로 컨텍스트로 준다 (값 자체를 매번 새로 만들지는 않아 같은 차량은 항상 같은 값으로 채점 가능)
- RAG 파이프라인(retriever.py): 레코드별로 차량번호·차종(+카테고리)·구분·소속·상태를 문장화해 Bedrock Titan 임베딩으로 변환하고, 인메모리 벡터 스토어(InMemoryVectorStore)에서 유사도 top-k 검색. 차량번호·부서 같은 정확 조회는 여전히 기존 필터링 도구를 쓰고, RAG는 필드에 없는 자연어 표현 전용
- 데이터 항목: 차량번호, 차주 이름(관리자 조회 전용), 차종, 입출차 시각, 부서/소속(방문객은 대신 방문 업체명), 이용 구분(임직원/방문객/정기권), 총 주차면수/잔여대수, 할인 대상 여부(장애인/국가유공자, 요금 계산 내부용이며 답변에는 노출하지 않음)
- 요금 정책: 정기권은 요금 없음. 방문객은 30분 무료, 임직원은 4시간 무료, 이후 30분당 1,000원이며 24시간마다 같은 기준으로 재적용. 무료 시간 내 출차는 0원(무료회차). 장애인·국가유공자 차량은 계산된 금액의 50% 할인(사유는 비공개)

## 4. 서비스 정책 (가드레일 요약)
- 등록되지 않은 차량번호는 지어내지 않고 없다고 안내한다
- 차단기 등 실제 설비를 조작하는 동작은 하지 않으며, 조회만 한다
- 일반 조회에서는 차량번호·부서 외의 개인정보(차주 이름 포함)를 답변에 담지 않는다. 관리자 전용 전체 차량 조회에서만 차주 이름을 포함한다
- 요금에 할인이 적용된 경우 원래 금액과 최종 금액은 안내하되, 왜 할인됐는지(장애인/국가유공자 등)는 답변에 담지 않는다

## 5. 성공 기준
- evaluation/test_queries.csv 25건(positive/negative/edge/guardrail) 중 90% 이상 통과
- 등록되지 않은 차량 질문에 지어낸 답이 0건
- evaluation/round1_report.md: RAG 응답(faithfulness/answer_relevancy/context_utilization)에 대한 RAGAS 자체 평가 기록
